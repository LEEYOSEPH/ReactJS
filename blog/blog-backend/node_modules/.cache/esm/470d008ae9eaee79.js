let Koa,Router,bodyParser,mongoose;_e9e‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_e9e‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_e9e‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_e9e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);require('dotenv').config();




//비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _e9e‍.g.console.log(e);
  });

const api = require('./api');

const app = new Koa();
const router = new Router();

//라우터 설정
router.use('/api', api.routes());

//라우터 적용 전에 bodyParser 적용
app.use(bodyParser());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

//PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _e9e‍.g.console.log('Listening to port %d', port);
});
